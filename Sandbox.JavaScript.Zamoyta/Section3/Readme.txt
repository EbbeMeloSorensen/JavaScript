Her arbjeder han bare videre på den js-demo applikation, der ligger i Section2-folderen

1) Han demonstrerer, hvordan man kan putte JavaScriptkode direkte ind på en html-side, men nævner,
   at det er bedst at putte den i en separat js-fil. Det har f.eks. den fordel, at flere forskellige
   web pages kan bruge samme javascript kode.
   Han demonstrerer hvordan man kan lave en simpel funktion og kalde den fra en anden JavaScript fil.
   Han næcner, at JavaScript er ligeglad med whitespace i modsætning til f.eks. Python og yml,
   men anbefaler, at man strukturerer det pænt. Han nævner også at JavaScript er case sensitivt.
   Til sidst viser han, hvordan man kommenterer. Det minder ret meget om CSharp.
   